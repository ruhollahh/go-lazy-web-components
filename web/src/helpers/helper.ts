export function addedLog(compName: string): string {
  return `${compName} is added to the dom`;
}

export function removedLog(compName: string): string {
  return `${compName} is removed to the dom`;
}

export function constructedLog(compName: string): string {
  return `${compName} is constructed`;
}
